simply `docker compose up` in both DB folders to get both database servers up and running

manually create a scheme called `todo_db` in both databases

do a `GET` on `http://localhost:3000/api/todos/populate` to populate both databases with mock data
do a `GET` on `http://localhost:3000/api/todos/statistics` to get statistics about performance in both databases

:-)
